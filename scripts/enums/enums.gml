enum dir {
	left,
	right,
	up,
	down,
}

enum follower {
	none,
	sam,
}

enum fadeSpeed {
	vslow = 2,
	slow = 5,
	med = 10,
	fast = 50
}

enum audioPriority {
	sfxlow,
	sfxmed,
	sfxhi,
	music
}

enum align {
	left,
	right,
	center
}
